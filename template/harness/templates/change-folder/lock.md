<!-- Written once by test-first, in its own commit directly after the commit
that adds the tests; read by scripts/cortex/tests-locked.sh. Never edited
afterward: the script fails if any later commit or uncommitted edit touches
this file. The listed files, every file matching TEST_GLOBS as of the named
commit, and .cortex/config itself are locked. -->

Tests-locked-at: <full sha of the commit that added the tests>

## Locked tests

- <path of each test or fixture file that commit created or changed>
