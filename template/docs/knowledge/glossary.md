# Glossary

Alphabetical. Each entry: **term** — one-sentence canonical definition —
what it is commonly confused with. `spec-clarify` checks proposals against
this file, so a term two people use differently belongs here.

<!-- TODO: seed with the terms that are overloaded in this codebase.
Example shape:

- **Settlement** — the point at which funds are irrevocably moved between
  accounts. Not the same as *authorization* (a hold) or *capture* (a request
  to move funds that can still fail).
-->
