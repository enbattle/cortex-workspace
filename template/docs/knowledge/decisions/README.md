# Decisions

One file per architecture decision, numbered `NNNN-slug.md`, written from
`harness/templates/adr.md`. Record a decision when it is costly to reverse or
would puzzle a future reader; routine choices live in the change folder's
`design.md`. A superseded decision stays, marked superseded, so the history
reads in order.
