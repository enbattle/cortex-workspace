# Architecture

What the components are, who owns each, and which way dependencies point.
Keep it to what a newcomer or an agent needs to place a change; the code is
the source of truth for details.

## Components

<!-- TODO: one row per component (in a monorepo, per package). -->

| Component | Path | Owner | Responsibility (one line) |
| --- | --- | --- | --- |
| | | | |

## Dependencies

<!-- TODO: "A calls B through <interface>". State any dependency that is
forbidden, e.g. "the UI never reads the database directly". A diagram
(ASCII or Mermaid) helps if there are more than a handful of components. -->

## External interfaces

<!-- TODO: what this system exposes (APIs, events, files) and what it
consumes, each with where its definition lives. A change that touches one of
these is interface-affecting: the proposal says so, and review checks
consumers. -->
