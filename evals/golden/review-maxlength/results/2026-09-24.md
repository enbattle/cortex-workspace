# review-maxlength results, 2026-09-24

- Fixture: `fixture.bundle` as first built today (`golden-clean` 9dd79ff,
  `golden-planted` b736d91, base e4446ed). This was the fixture's first run.
- cortex: branch `v2-single-repo-template`. The fixture was installed from
  ac6236f. By the time of grading, HEAD was 17f22d3, which changes only
  `docs/specs/2026-09-23-v2-scripts.md`, so `template/` and the fixture's
  harness match. The tree was dirty: this directory's files were uncommitted,
  and `.github/workflows/ci.yml` had changes that were not part of this run.
- Agent tool: Claude Code, with each run in a fresh `general-purpose`
  subagent (not a fork). The model was Opus 5.5, both for the subagents and
  for the grader. Each subagent got only the README's prompt and its own
  fresh clone, set up as in step 1. `gates: ok` and an empty status were
  confirmed before each run.

| Run | Tag | Verdict | Grade | Clone left clean |
| --- | --- | --- | --- | --- |
| planted-1 | golden-planted | request changes | PASS | yes (HEAD b736d91) |
| planted-2 | golden-planted | request changes | PASS | yes (HEAD b736d91) |
| clean-1 | golden-clean | approve | PASS | yes (HEAD 9dd79ff) |

**Task result: PASS.** Both planted runs passed and the control passed.

## planted-1

- Plant named at **High**: "F1 (High, introduced): truncating the raw input
  before slugifying breaks AC2/AC3/AC4 and proposal Q5". It cited
  `src/slugify.js` lines 31-36 and reproduced the bug with its own inputs.
  One was `' '.repeat(300) + 'Hello World'`, which gave `''` instead of
  `'hello'`. Another gave `'ab-cdef'`: a word was cut mid-word.
- Other findings:
  - F2 (Medium): the commit is unplanned, came after the gate output was
    recorded, and has no `## As built` entry. **Real**: it describes the same
    plant from the process side.
  - Already present: `String(input)` coercion, and arrays or functions
    accepted as `options`. Both are **correctly** not blocking, because the
    proposal lists them as non-goals.
- The probe scripts are in `runs/planted-1/probes/`, as instructed.

## planted-2

- Plant named at **High**: "truncating the input before slugifying breaks
  AC2, AC3 and AC5". It quoted Q5 and the design's "Build the full slug
  exactly as today". A fuzz run over 20,000 long, separator-heavy inputs
  found 7,210 wrong results.
- Other findings:
  - Medium: an unplanned commit after the gate output was recorded, with no
    `## As built` entry. **Real**: it is the same plant.
- The probe script is in `runs/planted-2/probes/`.

## clean-1 (control)

- Verdict: approve, with no introduced findings. There was one Low
  already-present item, `String(input)` coercion against P3, which the
  proposal names as a non-goal. **Real and correctly non-blocking.**
- It probed and found sound:
  - an exhaustive oracle comparison over strings of length 0-9 made from
    `{a, b, space, -}`, with `maxLength` 1-11;
  - AC6 and AC7 edge values, including `new Number(5)`, `5n`, `true` and
    `-0`;
  - a 1 MB input, which took about 45 ms;
  - the stage order and the lock.
- No false positives.

## Observations

- All three reviewers re-ran `gates.sh` themselves and noted that passing
  gates don't show the change is correct. Both planted reviewers also
  explained why the locked tests miss the plant: their inputs are at most
  40 characters.
- The fixture leaks one hint. The approval line and the commit author read
  "golden fixture". With the tags deleted, nothing else names the plant. The
  control approving suggests the hint doesn't trigger suspicion by itself.

# Refresh run, 2026-09-24 (fixture rebuilt from 15de2ea)

- Fixture: `fixture.bundle` rebuilt from the template at `15de2ea`, using
  that commit's `scripts/install.sh` and following its command texts
  literally. The change branch is `change/20260924-slugify-maxlength`.
  Commits: `golden-clean` b862feb, `golden-planted` 9896cce, base c77504a,
  and lock T 48758b4. The source, tests, proposal and planted content are
  the same as in the first build. At both tags `gates.sh` printed
  `gates: ok` and all 26 tests passed. The rubric's two reproductions give
  `'helloworld'` and `'hello-world'` at clean, and `'hellow'` and `''` at
  planted.
- cortex: HEAD 15de2ea, on branch `v2-single-repo-template`. The tree was
  dirty only in this directory: `fixture.bundle` and `README.md` had been
  refreshed and this file appended.
- Agent tool: Claude Code, with each run in a fresh `general-purpose`
  subagent (not a fork). The model was Opus 5.5, both for the subagents and
  for the grader. Each subagent got only the README's prompt and its own
  fresh clone, set up as in step 1. `gates: ok` and an empty status were
  confirmed before each run. The run directories were named neutrally
  (`run-a`, `run-b`, `run-c`) so the path gave no hint about the tag.

| Run | Tag | Verdict | Grade | Clone left clean |
| --- | --- | --- | --- | --- |
| planted-1 (run-a) | golden-planted | request changes | PASS | yes (HEAD 9896cce) |
| planted-2 (run-b) | golden-planted | request changes | PASS | yes (HEAD 9896cce) |
| clean-1 (run-c) | golden-clean | approve | PASS | yes (HEAD b862feb) |

**Task result: PASS.**

## planted-1 (run-a)

- Plant named at **High**: "H1. Pre-truncating the input breaks AC3, AC4
  and the prefix rule of AC5". It quoted Q5 and gave four reproductions.
  One was `'ab' + '.'.repeat(250) + 'cdefghij klm'` with `maxLength` 10,
  which gave `'ab-cdef'`. Another was a repeated padded input that gave 79
  characters instead of 99. The grader re-ran both and confirmed them.
- Other findings:
  - M1 (Medium): an unplanned commit after the gate output was recorded,
    with no `## As built` entry. **Real**: it describes the same plant from
    the process side.
  - Already present: `String(input)` coercion. Correctly not blocking,
    because the proposal lists it as a non-goal.

## planted-2 (run-b)

- Plant named at **High**: "the input pre-slice breaks AC2, AC3 and AC5
  and the proposal's clarification 5". Among its reproductions,
  `'abcde' + ' '.repeat(250) + 'fghijklmnop'` with `maxLength` 10 gave
  `'abcde-f'`, and the grader confirmed it. It ran 2,000 long random inputs
  against an oracle for the pre-plant logic and found no mismatches.
- Other findings: none, apart from a process note inside F1 (the commit came
  after the gate output and has no `## As built` entry). **Real**.

## clean-1 (control)

- Verdict: approve, with no High or Medium findings.
- One introduced Low finding: `['A b','C d'].map(slugify)` now throws,
  because `map` passes the index as `options`, and the proposal's "compatible
  under E2" wording glosses over this. The grader reproduced it. **Real but
  arguable**: design.md records that a number passed as the second argument
  now throws and that the user accepted it. It is correctly non-blocking.
- It probed and found sound:
  - a 20,000-input oracle fuzz with a non-ASCII alphabet, run at 11 limits;
  - hostile `maxLength` and `options` values;
  - a 10 MB input, which took about 0.4 s;
  - the stage order and the lock.
- No false positives.

## Observations

- Running the command texts literally raised no blocking ambiguity. Minor
  points:
  - `spec-new` step 1 uses `<yyyymmdd>-<slug>` in the branch name before it
    defines it, in the folder clause that follows.
  - `INSTALL.md` step 7 leaves merging `cortex-install` to the user, and
    the fixture builder did it as the user.
  - Step 5b (CI) was skipped because the fixture isn't hosted.
  - `install.sh`'s core.filemode note was needed on Windows:
    `git update-index --chmod=+x` was run before the install commit.
- `test-first`'s precondition ("its latest commit contains the approved
  change folder") and its three-commit sequence were followed as written,
  and `tests-locked.sh` passed after the third commit.
